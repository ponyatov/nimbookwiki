# переменные среды

## `$PATH`

```sh
~$ grep -i nim ~/.setenv 
# Nim
export PATH="$HOME/.nimble/bin:$PATH"

~$ grep -i setenv ~/.profile 
. ~/.setenv

~$ grep -i setenv ~/.xsessionrc 
. ~/.setenv

```
